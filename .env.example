# Persistence config.
# Leave unset to play fully offline: the game falls back to browser localStorage.
# Set POSTGRES_URL (or DATABASE_URL) to enable the server-side @vercel/postgres backend.
POSTGRES_URL=postgresql://user:password@host:5432/dbname
# DATABASE_URL=postgresql://user:password@host:5432/dbname
